---
description: Build or review a tycoon using genre patterns without adding unrequested systems.
agent: roblox-dev
---

Use `roblox-agent-audits` genre template guidance for a tycoon request:

```text
$ARGUMENTS
```

Rules:

- Treat tycoon patterns as optional context: plot claiming, droppers, collectors, buttons, income, upgrades.
- Implement only what the user asked for.
- Do not add monetization, rebirth, leaderboard, autosave, shop UI, or extra tiers unless requested.
- Ask if plot ownership, save behavior, or multiplayer scope is unclear.
- Verify before/after if Studio is modified.

Answer in Spanish.
